services:
  lantu-epay-adapter-v2:
    image: ghcr.io/nkbaa/lantu-epay-adapter-v2:latest
    container_name: lantu-epay-adapter-v2
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-18080}:${PORT:-18080}"
